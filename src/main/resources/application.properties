# MySQL Database Configuration
db.url=jdbc:mysql://localhost:3306/javatalk?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
db.username=
db.password=

# Redis Configuration
redis.host=localhost
redis.port=6379
redis.password=

# Server Configuration
server.port=60033
server.host=0.0.0.0
server.ssl.enabled=true

# SSL Configuration (only used when server.ssl.enabled=true)
# Using PKCS12 format (supported by Java 9+)
ssl.keystore.path=keystore.p12
ssl.keystore.password=changeit
ssl.key.password=changeit

# Cache Configuration
cache.ttl.message=604800
cache.message.limit=500
